---
id: dapps
title: DApps
sidebar_position: 1
slug: /
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";
import tutorial from "@theme/TabItem";
import Card from "@components/Card";
import CardLayout from "@components/CardLayout";
import TutorialFrame from "@components/TutorialFrame";
import MoreCard from "@components/MoreCard";
import {
  tutorialCardContents,
  toolCardContents,
  tutorialFrameContent,
  MORETOOLCARDTITLE,
  MORETOOLCARDHREF,
} from "./CardsContents";

# DApps

#### Your go-to resources for dApp development on CKB.

## Examples

The examples below show in code how dApps interact with CKB TestNet contracts, along with detailed tutorials explaining how to custom/build these dApps on your local CKB dev blockchain.

##### Note: Never use these address in production, or share your private key with anyone.

<Tabs>
  {tutorialFrameContent.map(
    ({ tabValue, label, tutorialTitle, tutorialLink, iframeSrc }, index) => (
      <TabItem value={tabValue} label={label} default={index === 0}>
        <TutorialFrame
          tutorialTitle={tutorialTitle}
          tutorialLink={tutorialLink}
          iframeSrc={iframeSrc}
        />
      </TabItem>
    )
  )}
</Tabs>

## Local DApp Development Tutorials

The tutorials below focuses on developing dApps using a local dev CKB blockchain.

<CardLayout>
  {tutorialCardContents.map(({ index, title, description, href, type }) => (
    <Card
      key={index}
      title={title}
      description={description}
      href={href}
      type={type}
    />
  ))}
</CardLayout>

## Dev Tools

Tools tailored for dApp development to deployment on CKB.

<CardLayout>
  {toolCardContents
    .slice(0, 3)
    .map(({ index, title, description, href, type, links }) => (
      <Card
        key={index}
        title={title}
        description={description}
        href={href}
        type={type}
        links={links}
      />
    ))}
  <MoreCard title={MORETOOLCARDTITLE} href={MORETOOLCARDHREF} />
</CardLayout>
